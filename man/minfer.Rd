% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minfer.R
\name{minfer}
\alias{minfer}
\title{This function processes a list of datasets (stored in an RData file), checks for the presence 
of specified metabolite IDs in the rows and columns, and constructs result matrices based on 
interactions (1 or 0) between metabolites.}
\usage{
minfer(input_ids, rdata_file = "MetaboAnalystMINs.RData")
}
\arguments{
\item{input_ids}{A character vector of metabolite IDs to analyze. These IDs should be present in 
both the row and column names of each dataset.}

\item{rdata_file}{A string specifying the path to the RData file containing the `data_list` object. 
This list should include datasets as matrices or data frames representing 
metabolite interactions.}
}
\value{
A list of result matrices, one for each dataset in `data_list`. Each matrix has the 
        metabolites as rows and columns, with values:
        - 1: Interaction exists
        - 0: No interaction
        - NA: Missing data
}
\description{
This function processes a list of datasets (stored in an RData file), checks for the presence 
of specified metabolite IDs in the rows and columns, and constructs result matrices based on 
interactions (1 or 0) between metabolites.
}
\examples{
# Define metabolite IDs
input_ids <- c('C00042', 'C00149', 'C00036')

# Analyze the data
results <- minfer(input_ids)

# Print results for each dataset
for (i in 1:length(results)) {
    cat("Results for dataset ", i, ":\n")
    print(results[[i]])
    cat("\n")
}
}
